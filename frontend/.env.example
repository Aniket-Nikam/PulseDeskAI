# Leave unset to use same-origin /api proxy in local dev.
# VITE_API_URL=http://localhost:8000/api/v1

# Set this only when frontend and backend are on different public origins.
# Example:
# VITE_API_URL=https://your-public-api-domain/api/v1
